# Next.js configuration

Keep framework configuration small. Security headers belong close to the
deployment boundary.

<!-- artifact: next.config.ts; profiles: base,forms,data,auth,realtime,full -->
```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  async headers() {
    return [
      {
        source: "/(.*)",
        headers: [
          { key: "X-Content-Type-Options", value: "nosniff" },
          { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
          { key: "Permissions-Policy", value: "camera=(), microphone=(), geolocation=()" },
        ],
      },
    ];
  },
};

export default nextConfig;
```

<!-- artifact: next-env.d.ts; profiles: base,forms,data,auth,realtime,full -->
```ts
/// <reference types="next" />
/// <reference types="next/image-types/global" />

// This file is generated by Next.js in ordinary applications.
```
