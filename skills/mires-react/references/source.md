# Source

- Public skill name: `mires-react`
- Handbook: `references/handbook/`

The handbook originated as the standalone `react-personal-references` repository and was merged into Mires. It is now first-class content of this repository: edit the Markdown under `references/handbook/` directly.

The handbook keeps its own maintainer contract in `references/handbook/maintainer-instructions.md`: Markdown is the canonical source, complete files live in fenced artifacts, and reconstructed output is disposable and never committed.
